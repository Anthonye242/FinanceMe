<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Budget Goal Details</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
  <h1>Budget Goal Details</h1>
  <p><strong>Category:</strong> <%= budget.category %></p>
  <p><strong>Amount:</strong> $<%= budget.amount %></p>
  <p><strong>Start Date:</strong> <%= budget.startDate.toDateString() %></p>
  <p><strong>End Date:</strong> <%= budget.endDate.toDateString() %></p>
  <a href="/users/<%= user._id %>/budget/<%= budget._id %>/edit">Edit</a>
  <form action="/users/<%= user._id %>/budget/<%= budget._id %>?_method=DELETE" method="POST">
    <button type="submit">Delete</button>
  </form>
  <a href="/users/<%= user._id %>/budget">Back to Budget Goals</a>
</body>
</html>
