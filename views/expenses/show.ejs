<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expense Details</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
    <h1>Expense Details</h1>
    <p>Amount: <%= expense.amount %></p>
    <p>Category: <%= expense.category %></p>
    <p>Date: <%= expense.date.toDateString() %></p>
    <p>Description: <%= expense.description %></p>
    <a href="/users/<%= user._id %>/expenses/<%= expense._id %>/edit">Edit</a>
    <form action="/users/<%= user._id %>/expenses/<%= expense._id %>?_method=DELETE" method="POST">
      <button type="submit">Delete</button>
    </form>
    <a href="/users/<%= user._id %>/expenses">Back to Expenses</a>
</body>
</html>
